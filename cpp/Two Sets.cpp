#include <iostream>

int main()
{
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int n;
    std::cin >> n;
    if (n % 4 != 0) {
        std::cout << "NO\n";
        return 0;
    }
    std::cout << "YES\n";
    for (int )
}